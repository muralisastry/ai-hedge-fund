# ---------------------------------------------------------------------------
# Data sources
# ---------------------------------------------------------------------------
# The data layer is provider-routed (src/tools/providers). Default per type is
# Financial Datasets (`fd`); flip a type to a cheaper source once validated:
#   internal = shared quantai-market-data Postgres (Polygon-backed) + fundamentals
#   polygon  = Polygon API directly (news + financials vX)
#   sec      = SEC EDGAR (insider Form 4, companyfacts line-item fallback)
#   metrics  = ratios computed locally from raw line items + price
# Global default, then per-type overrides:
DATA_PROVIDER=fd
# DATA_PROVIDER_PRICES=internal
# DATA_PROVIDER_MARKET_CAP=internal
# DATA_PROVIDER_COMPANY_NEWS=polygon
# DATA_PROVIDER_INSIDER_TRADES=sec
# DATA_PROVIDER_LINE_ITEMS=internal
# DATA_PROVIDER_FINANCIAL_METRICS=metrics
# Keep FD as automatic fallback during migration; set false after full cutover:
DATA_PROVIDER_FALLBACK_FD=true

# Shared internal Postgres (every suite app uses this; Polygon-backed):
QUANTAI_MARKETDATA_DB_URL=postgresql://muralisastry@localhost:5432/quantai_marketdata
# Polygon (already part of the suite subscription — $0 marginal):
POLYGON_API_KEY=your-polygon-api-key
# SEC EDGAR asks for a descriptive User-Agent (free, no key):
SEC_EDGAR_USER_AGENT=quantai-trading you@example.com

# Financial Datasets — expensive ($200-2000/mo). Kept only as the migration
# fallback/parity baseline; remove once every type is validated off `fd`.
# Get a key at https://financialdatasets.ai/
FINANCIAL_DATASETS_API_KEY=your-financial-datasets-api-key

# For running LLMs hosted by openai (GPT 5, etc.)
# Get your OpenAI API key from https://platform.openai.com/
OPENAI_API_KEY=your-openai-api-key

# For running LLMs hosted by anthropic (claude-4-sonnet, claude-4.1-opus, etc.)
# Get your Anthropic API key from https://anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key

# For running LLMs hosted by deepseek (deepseek-chat, deepseek-reasoner, etc.)
# Get your DeepSeek API key from https://deepseek.com/
DEEPSEEK_API_KEY=your-deepseek-api-key

# For running LLMs hosted by groq (deepseek, llama3, etc.)
# Get your Groq API key from https://groq.com/
GROQ_API_KEY=your-groq-api-key

# For running LLMs hosted by gemini (gemini-2.5-flash, gemini-2.5-pro)
# Get your Google API key from https://ai.dev/
GOOGLE_API_KEY=your-google-api-key

# For running LLMs hosted by xAI (Grok 4, etc.)
XAI_API_KEY=your-xai-api-key

# For running LLMs hosted by Moonshot / Kimi (kimi-k2, kimi-latest, moonshot-v1-*)
# Get your Moonshot API key from https://platform.moonshot.ai/
# Mainland China users: set MOONSHOT_BASE_URL=https://api.moonshot.cn/v1
MOONSHOT_API_KEY=your-moonshot-api-key

# For running LLM GigaChat
GIGACHAT_API_KEY=your-gigachat-api-key

# For running LLMs hosted by OpenRouter
OPENROUTER_API_KEY=your-openrouter-api-key

# For running LLMs hosted by Microsoft Azure OpenAI (gpt-4o, gpt-4o-mini, etc.)
# Get your Azure OpenAI API settings from https://oai.azure.com/
AZURE_OPENAI_API_KEY=your-azure-openai-api-key
AZURE_OPENAI_ENDPOINT=your-azure-openai-endpoint
AZURE_OPENAI_DEPLOYMENT_NAME=your-azure-openai-deployment-name